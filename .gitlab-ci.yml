stages: 
  - build
  - test

default:
  image: haskell:8.10.2
  before_script:
    - cabal update

build:
  stage: build
  script: cabal build

test: 
  stage: test
  script: cabal test --test-show-details=streaming --test-option=--color
