stages: 
  - build
  - test

default:
  image: rp218/haskell_v4l2:latest
  before_script:
    - cabal update

build:
  stage: build
  script: cabal build

test: 
  stage: test
  script: cabal test --test-show-details=streaming --test-option=--color
